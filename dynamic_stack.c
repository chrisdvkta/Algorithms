#include <stdio.h>
#include <stdbool.h>


struct Node {
    int data; 
    struct Node *next; 
};


void initializeStack (struct Node *N){
    struct Node *N = NULL; 
}

int isFull(struct Node *N){
    struct Node new_node = new Node(); 
}